import { Close, CloudUpload, Favorite, FavoriteBorder, Person, PersonAdd, Visibility, VisibilityOff } from "@mui/icons-material"
import { Avatar, Box, Button, Card, CardContent, CardHeader, CardMedia, Divider, FormControl, FormControlLabel, FormHelperText, Grid, IconButton, Input, InputAdornment, InputLabel, List, ListItem, ListItemAvatar, ListItemText, Menu, MenuItem, Modal, OutlinedInput, Stack, Table, TableBody, TableCell, TableContainer, TableHead, TableRow, TextField, Typography } from "@mui/material"
import { Form, Formik } from "formik"
import moment from "moment"
import { useDispatch, useSelector } from "react-redux"
import { Link, Navigate, Outlet, useLocation, useNavigate } from "react-router-dom"
import { Comment, Label, Send, Share } from "@mui/icons-material";



 const Index = {
    Box,
    TextField,
    Typography,
    FormControl,
    OutlinedInput,
    List,
    ListItem,
    InputLabel,
    Grid,
    Link,
    FormHelperText,
    Button,
    Modal,
    Table,
    TableContainer,
    TableHead,
    TableRow,
    TableBody,
    TableCell,
    InputAdornment,
    IconButton,
    Visibility,
    VisibilityOff,
    FormControlLabel,
    Menu,
    MenuItem,
    Card,
    CardContent,
    CardHeader,
    CardMedia,
    ListItemAvatar,
    Divider,
    ListItemText,
    Stack,
    Input,


    // icon
    Avatar,
    Person,
    Favorite,
    PersonAdd,
    FavoriteBorder,
    CloudUpload,
    Close,
    Comment,
    Label,
    Send,
    Share,

    useSelector,
    useDispatch,
    useNavigate,
    useLocation,
    Outlet,
    Navigate,
    moment,
    Form,
Formik,

}
export default Index